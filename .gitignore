# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Configuration files (if they contain user-specific data)
# Uncomment the next line if config.json contains personal data
# config.json

# IDE
.idea/
*.swp
*.swo
*~
# Keep .vscode/ since it contains useful Fusion 360 debug config

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log

# Node / UI. ui/ is the source; ui_dist/ is the build output and IS
# committed, so the add-in loads from a fresh clone without anyone running
# npm.
ui/node_modules/
ui/.vite/
